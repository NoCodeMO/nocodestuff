{
  "name": "afterlight-bunker",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "test": "node scripts/validate.js && node scripts/landscape-layout-check.js && node scripts/combat-balance.js && node scripts/merchant-balance.js && node scripts/room-balance.js && node scripts/mission-balance.js && node scripts/command-center-check.js && node scripts/offline-balance.js && node scripts/codex-check.js",
    "test:browser": "bash scripts/smoke.sh",
    "test:all": "npm test && npm run test:browser"
  }
}
