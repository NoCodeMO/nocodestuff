{
  "name": "afterlight-bunker",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "test": "node scripts/validate.js && node scripts/combat-balance.js && node scripts/merchant-balance.js",
    "test:browser": "bash scripts/smoke.sh",
    "test:all": "npm test && npm run test:browser"
  }
}
