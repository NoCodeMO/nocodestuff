'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm');
const root=path.resolve(__dirname,'..'),read=file=>fs.readFileSync(path.join(root,file),'utf8'),fail=message=>{throw new Error(`Late-game economy: ${message}`)};
const context={window:{}};vm.runInNewContext(read('js/core/config.js'),context);vm.runInNewContext(read('js/core/economy.js'),context);
const cfg=context.window.BunkrConfig,economy=context.window.BunkrEconomy,assert=(condition,message)=>{if(!condition)fail(message)};
const earlyWorkshop=economy.roomCostAt('workshop',62,1),expectedFloor=Math.floor(cfg.ROOMS.workshop.base*Math.pow(cfg.ROOM_ECONOMY.costGrowth,62));
assert(earlyWorkshop>expectedFloor,'the calibrated cost ramp must slow the opening without changing monotonic growth');
const lateGenerator=economy.roomCostAt('generator',1979,.6),nextGenerator=economy.roomCostAt('generator',1980,.6);
assert(Number.isFinite(lateGenerator)&&lateGenerator>1e110,'level 1979 must have a real finite Mastery-era price instead of the old 9.01Qa clamp');
assert(nextGenerator>lateGenerator&&Math.abs(nextGenerator/lateGenerator-cfg.ROOM_ECONOMY.costGrowth)<1e-10,'late-game prices must keep growing at the configured Mastery rate');
assert(lateGenerator!==Number.MAX_SAFE_INTEGER,'room prices may never clamp to Number.MAX_SAFE_INTEGER again');
const threeLevels=economy.roomUpgradeQuote('generator',3,1979,.6),screenshotCoins=threeLevels.cost*1.01,firstMax=economy.affordableRoomLevels('generator',screenshotCoins,1979,.6,500);
assert(firstMax.count===3,'a calibrated level-1979 balance must receive an exact bounded MAX quote');
assert(economy.validPurchase(firstMax,screenshotCoins,1979),'the bounded Mastery-era MAX quote must be transaction-safe');
const remaining=economy.subtractPurchase(screenshotCoins,firstMax.cost),repeatMax=economy.affordableRoomLevels('generator',remaining,firstMax.to,.6,500);
assert(Number.isFinite(remaining)&&remaining>=0&&remaining<screenshotCoins,'a MAX purchase must deduct a finite positive amount');
assert(repeatMax.count===0,'the same balance must not buy unlimited repeated MAX upgrades');
for(const badCost of [Infinity,NaN,0,-1])assert(!economy.validPurchase({count:1,cost:badCost,from:10,to:11},1e200,10),'invalid prices must never pass purchase validation');
assert(economy.sanitizeResource(Infinity)===cfg.ROOM_ECONOMY.maximumValue,'positive overflow must cap safely instead of becoming infinite money');
assert(economy.safeAdd(cfg.ROOM_ECONOMY.maximumValue,1e299)===cfg.ROOM_ECONOMY.maximumValue,'resource additions must stay below the finite game ceiling');
const game=read('js/core/game.js'),state=read('js/core/state.js'),html=read('index.html');
assert(!/Number\.MAX_SAFE_INTEGER/.test(game),'the exploitable 9.01Qa room-price ceiling still exists in game.js');
assert(/ECON\.validPurchase\(quote,state\.coins,current\)/.test(game),'room purchases need an in-transaction price and balance guard');
assert(/state\[k\]=ECON\.sanitizeResource\(state\[k\]\)/.test(state),'loaded resources must be repaired when they contain overflow values');
assert(/lateGameEconomyTest/.test(state)&&/state\.rooms\.generator=1979/.test(state),'the localhost regression fixture must reproduce the reported save in browser tests');
assert(html.indexOf('js/core/economy.js?build=3')<html.indexOf('js/core/state.js?build=28'),'the safe economy module must load before save-state normalization');
console.log(`Bunkr late-game economy passed: LV 1979 costs ${lateGenerator.toExponential(3)}, calibrated MAX buys ${firstMax.count} level(s), and repeated MAX is blocked.`);
