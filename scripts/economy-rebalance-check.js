'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm');
const root=path.resolve(__dirname,'..'),read=file=>fs.readFileSync(path.join(root,file),'utf8'),assert=(condition,message)=>{if(!condition)throw new Error(`Economy rebalance: ${message}`)};
const context={window:{},document:{body:{dataset:{}}},CustomEvent:function(type,options){this.type=type;this.detail=options?.detail},console};context.window.dispatchEvent=()=>{};vm.runInNewContext(read('js/core/config.js'),context,{filename:'config.js'});vm.runInNewContext(read('js/core/economy.js'),context,{filename:'economy.js'});
const cfg=context.window.AfterlightConfig,economy=context.window.AfterlightEconomy,rules=cfg.ROOM_ECONOMY,rooms=cfg.ROOMS,milestones=cfg.ROOM_MILESTONES;
const operationState={rooms:{},operations:{priorities:{...cfg.OPERATIONS.defaultPriority},paused:{}},coins:0,food:0,water:0,power:0,scrap:0,science:0};context.window.AfterlightState={get:()=>operationState,update:(reason,fn)=>{fn(operationState);return operationState}};vm.runInNewContext(read('js/systems/operations.js'),context,{filename:'operations.js'});const operations=context.window.AfterlightOperations;
assert(rules.masterySize===100&&rules.maximumRoomLevel>=5000,'rooms need repeatable 100-level Mastery ranks and future Prestige headroom');
assert(JSON.stringify([...cfg.PRESTIGE.targets])===JSON.stringify([100,200,325,525,850]),'Prestige I–V target curve drifted');
assert(cfg.PRESTIGE.targetFormula.growth>1.5&&cfg.PRESTIGE.targetFormula.growth<1.7,'future Prestige growth must remain near-exponential but tunable');
assert(JSON.stringify(milestones.map(item=>item.level))===JSON.stringify([5,10,25,50,75,100]),'each Mastery rank needs six readable milestones');
assert(economy.roomCostAt('generator',2)>economy.roomCostAt('generator',1)&&economy.roomCostAt('generator',101)>economy.roomCostAt('generator',100),'room prices must grow across Mastery boundaries');
function milestone(local){let result=1;for(const entry of milestones)if(local>=entry.level)result=entry.multiplier;return result}
function roomRate(id,resource,level,multiplier=1){if(!level)return 0;const size=rules.masterySize,rank=Math.floor((level-1)/size),local=1+(level-1)%size,last=milestones[milestones.length-1],rankScale=size*Math.pow(rules.rateGrowth,size-1)*last.multiplier;return (rooms[id].prod[resource]||0)*Math.pow(rankScale,rank)*local*Math.pow(rules.rateGrowth,local-1)*milestone(local)*multiplier}
function simulatedHours(target,productionMultiplier=1,costMultiplier=1){const levels=Object.fromEntries(Object.keys(rooms).map(id=>[id,id==='generator'?1:0]));Object.assign(operationState,{rooms:levels,coins:0,food:0,water:0,power:0,scrap:0,science:0,operations:{priorities:{...cfg.OPERATIONS.defaultPriority},paused:{}}});let seconds=0;const bunker=()=>1+Math.floor(Object.values(levels).reduce((a,b)=>a+b,0)/rules.bunkerLevelEvery),rates=()=>{const roomRates=Object.fromEntries(Object.keys(rooms).map(id=>[id,Object.fromEntries(['coins','food','water','power','scrap','science'].map(resource=>[resource,roomRate(id,resource,levels[id],productionMultiplier)]))])),result=operations.evaluate(roomRates,{coins:0,food:0,water:0,power:0,scrap:0,science:0});result.rates.coins+=.1*productionMultiplier;return result.rates};while(bunker()<target){const available=Object.keys(rooms).filter(id=>bunker()>=rooms[id].unlock),choice=available.map(id=>({id,cost:economy.roomCostAt(id,levels[id],costMultiplier)})).sort((a,b)=>a.cost-b.cost)[0],income=Math.max(.1,rates().coins),wait=Math.max(0,(choice.cost-operationState.coins)/income);seconds+=wait;operationState.coins=Math.max(0,operationState.coins+wait*income-choice.cost);levels[choice.id]++;for(const resource of ['food','water','power','scrap','science'])operationState[resource]=0;assert(Number.isFinite(seconds)&&seconds<365*86400,'simulation exceeded one year or overflowed')}return seconds/3600}
const hours=cfg.PRESTIGE.targets.map((target,index)=>simulatedHours(target,Math.pow(cfg.PRESTIGE.economyPerLevel,index)*(1+index*cfg.PRESTIGE.rosterBonusPerLevel),index?.9:1));
assert(hours[0]>=18&&hours[0]<=36,`fresh Prestige I passive baseline should be roughly one passive day after operating costs, got ${hours[0].toFixed(1)}`);
assert(hours.slice(1,4).every(value=>value>=24&&value<=120),`Prestige II–IV passive baselines must stay near the 1–4 day band before active rewards: ${hours.map(v=>v.toFixed(1)).join(', ')}`);assert(hours[4]>=72&&hours[4]<=168,`Prestige V must remain a high-cycle challenge below one passive week, got ${hours[4].toFixed(1)} hours`);
const offers=Object.fromEntries(cfg.MERCHANT_OFFERS.map(offer=>[offer.id,offer]));
assert(offers.gold5.cost===25&&offers.gold10.cost===60&&offers.all3.cost===75&&offers.scrap5.cost===30&&offers.hunter.cost===40&&offers.lure.cost===50,'Dealer costs must stay in the approved 6–9x rebalance');
assert(offers.gold5.group===offers.gold10.group&&offers.gold10.group===offers.all3.group,'Coin and everything boosts must be mutually exclusive');
const missions=read('js/systems/missions.js');
for(const marker of ['BONUS_LIMITS','balanceVersion','rebuiltBonuses','boundedBonus'])assert(missions.includes(marker),`mission normalization is missing ${marker}`);
assert(/prodMult:2\.5/.test(missions)&&/costMult:\.75/.test(missions),'mission production and discount caps changed');
const state=read('js/core/state.js'),command=read('js/systems/command-center.js'),css=read('app.css');
assert(/schema:19/.test(state)&&/function resetAll\(token\)/.test(state)&&/resetInProgress/.test(state),'schema 19 and autosave-safe full reset API are required');
assert(/TYPE RESET TO ARM THE BUTTON/.test(command)&&/3000\)/.test(command)&&/setPointerCapture/.test(command)&&/data-command-reset-open/.test(command),'Account reset needs typed confirmation and a touch-stable three-second hold');
assert(css.includes('.accountResetModal')&&css.includes('@keyframes accountResetHold'),'Account reset presentation is missing');
console.log(`Afterlight economy rebalance passed: P1–V passive baselines ${hours.map(value=>value.toFixed(1)+'h').join(' / ')}, 100-level Masteries and bounded Dealer/missions.`);
