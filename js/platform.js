(()=>{'use strict';
const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true,isiOS=/iphone|ipad|ipod/i.test(navigator.userAgent);document.documentElement.classList.toggle('standalone-app',standalone);
function banner(){if(standalone||document.getElementById('appModeBanner'))return;const b=document.createElement('div');b.id='appModeBanner';b.innerHTML=isiOS?`<div><b>AFTERLIGHT APP MODE</b><span>Open fullscreen without Safari controls.</span></div><button id="appModeBtn">HOW TO INSTALL</button><button id="appModeX">×</button>`:`<div><b>AFTERLIGHT FULLSCREEN</b><span>Hide browser controls and play like an app.</span></div><button id="appModeBtn">ENTER FULLSCREEN</button><button id="appModeX">×</button>`;document.body.append(b);document.getElementById('appModeX').onclick=()=>b.remove();document.getElementById('appModeBtn').onclick=()=>{if(isiOS){const modal=document.createElement('div');modal.className='installModal';modal.innerHTML=`<div><small>INSTALL AFTERLIGHT</small><h2>PLAY LIKE A REAL APP</h2><p>1. Tap the <b>Share</b> button in Safari.</p><p>2. Choose <b>Add to Home Screen</b>.</p><p>3. Open <b>Afterlight</b> from your Home Screen.</p><p class="hint">Safari bars disappear and the game launches in standalone app mode.</p><button>CLOSE</button></div>`;document.body.append(modal);modal.querySelector('button').onclick=()=>modal.remove()}else{const root=document.documentElement,p=root.requestFullscreen||root.webkitRequestFullscreen;if(p)Promise.resolve(p.call(root)).catch(()=>{})}}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(banner,700));else setTimeout(banner,700);window.AfterlightPlatform={standalone,isiOS};
})();