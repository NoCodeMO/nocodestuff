(()=>{'use strict';const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;const isiOS=/iphone|ipad|ipod/i.test(navigator.userAgent);document.documentElement.classList.toggle('standalone-app',standalone);function banner(){if(standalone||document.getElementById('appModeBanner'))return;let b=document.createElement('div');b.id='appModeBanner';b.innerHTML=isiOS?`<div><b>AFTERLIGHT APP MODE</b><span>Open fullscreen without Safari controls.</span></div><button id="appModeBtn">HOW TO INSTALL</button><button id="appModeX">×</button>`:`<div><b>AFTERLIGHT FULLSCREEN</b><span>Hide browser controls and play like an app.</span></div><button id="appModeBtn">ENTER FULLSCREEN</button><button id="appModeX">×</button>`;document.body.append(b);document.getElementById('appModeX').onclick=()=>b.remove();document.getElementById('appModeBtn').onclick=()=>{if(isiOS){let m=document.createElement('div');m.className='installModal';m.innerHTML=`<div><small>INSTALL AFTERLIGHT</small><h2>PLAY LIKE A REAL APP</h2><p>1. Tap the <b>Share</b> button in Safari.</p><p>2. Choose <b>Add to Home Screen</b>.</p><p>3. Open <b>Afterlight</b> from your Home Screen.</p><p class="hint">Safari bars disappear and the game launches in standalone app mode.</p><button>CLOSE</button></div>`;document.body.append(m);m.querySelector('button').onclick=()=>m.remove()}else{let el=document.documentElement;(el.requestFullscreen||el.webkitRequestFullscreen)?.call(el).catch?.(()=>{})}}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(banner,700));else setTimeout(banner,700);})();